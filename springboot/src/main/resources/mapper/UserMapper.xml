<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.dao.UserDao">
<select id="selectAll" resultType="com.example.springboot.entity.User">
    select * from user
<where>
    <if test="username != null"> and username like concat('%', #{username}, '%')</if>
</where>
</select>
<insert id="insertUser">
    insert into user (username,password,avatar,name,sex,age,phone,role)
    values(#{username},#{password},#{avatar},#{name},#{sex},#{age},#{phone},#{role})
</insert>
    <delete id="deleteById">
        delete from user
        where  id = #{id}
    </delete>


    <update id="updateById" parameterType="com.example.springboot.entity.User">
        update user
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="avatar != null">
                avatar = #{avatar},
            </if>
            <if test="role != null">
                role = #{role},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="selectUserByUsername" resultType="com.example.springboot.entity.User">
        select user.*
        from user
        where username = #{username}
    </select>
    <select id="selectById" resultType="com.example.springboot.entity.User">
        select user.*
        from user
        where id = #{id}
    </select>

</mapper>